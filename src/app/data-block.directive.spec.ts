import { BlockDirective } from './data-block.directive';

describe('BlockDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockDirective();
    expect(directive).toBeTruthy();
  });
});
